git init
git add . 
git rm --cached <file_name> 
git commit -m "  "
git restore <file_name>

git config --global user.email "mailid"
git config --global user.name "username"

history

git -ls-files
git status <file_name>

git remote 
git remote -v

git remote set-url origin https://<personalaccesstoken>@github.com/mayurpatil8008/github-for-devops-workshop.git
git branch -M master
git push -u origin master

git clone <github code url>